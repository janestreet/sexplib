(library
 ((name sexplib)
  (libraries (bigarray parsexp sexplib0))
  (public_name sexplib)
  (preprocess no_preprocessing)
))

(ocamllex (lexer))
(ocamlyacc (parser parser_with_layout))


(jbuild_version 1)
